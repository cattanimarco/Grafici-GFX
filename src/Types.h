#ifndef GRAFICI_TYPES_H
#define GRAFICI_TYPES_H

#include "Arduino.h"

using DisplayNorm = float;  /* display coordinate normalized to the dimension size */
using DisplayAbd = int16_t; /* display coordinate in absolute value */
using DataNorm = float;
using ColorUint8 = unsigned char;
using ColorGFX = uint16_t;

#endif /* GRAFICI_TYPES_H */